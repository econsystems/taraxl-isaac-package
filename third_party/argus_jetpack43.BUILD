# This package contains the aarch64 version of the argus package

cc_library(
    name = "argus_aarch64_jetpack43",
    srcs = glob(["lib/*.so"]),

    hdrs = glob(["*.h*"])+
           glob(["include/**/*.h*"]),
    includes = [ "include/", "."],
    visibility = ["//visibility:public"],
   
)
